<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

        <import type="android.view.View" />

        <variable
            name="model"
            type="net.wildfyre.client.viewmodels.PostFragmentViewModel" />
    </data>

    <androidx.coordinatorlayout.widget.CoordinatorLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <ProgressBar
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:visibility="@{safeUnbox(model.contentLoaded) ? View.GONE : View.VISIBLE}" />

        <net.wildfyre.client.views.markdown.MarkdownRecyclerView
            android:id="@+id/content"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_marginBottom="@dimen/comments_header_height"
            android:clipChildren="false"
            android:clipToPadding="false"
            android:orientation="vertical"
            android:paddingLeft="@dimen/margin_horizontal_large"
            android:paddingTop="@dimen/margin_vertical_medium"
            android:paddingRight="@dimen/margin_horizontal_large"
            android:paddingBottom="@dimen/margin_vertical_medium"
            app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager" />

        <include
            android:id="@+id/collapsible_comments"
            layout="@layout/fragment_post_comments"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_gravity="bottom"
            app:behavior_hideable="false"
            app:behavior_peekHeight="@dimen/comments_header_height"
            app:behavior_skipCollapsed="false"
            app:layout_behavior="com.google.android.material.bottomsheet.BottomSheetBehavior"
            app:model="@{model}" />
    </androidx.coordinatorlayout.widget.CoordinatorLayout>
</layout>
